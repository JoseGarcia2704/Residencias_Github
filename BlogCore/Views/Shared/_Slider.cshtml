@model BlogCore.Models.ViewModels.HomeVM

<!--Slider-->

@* <div id="carouselExampleControls" class="carousel slide custom-carousel" data-bs-ride="carousel" style="max-height: 400px; overflow: hidden;">
    <div class="carousel-inner">

        @{
            int cont = 0;
        }
        @foreach (var item in Model.Slider)
        {
            @if (item.Estado == true)
            {
                var itemDinamico = cont++ == 0 ? " active" : "";

                <div class="carousel-item @itemDinamico">
                    <img src="@Url.Content(item.UrlImagen)" class="d-block w-100" alt="@Html.Raw(item.Nombre)" style="object-fit: cover; max-height: 300px;">
                    <div class="carousel-caption text-center">
                        <h1 class="text-white" style="font-size: 1.5rem;">@Html.Raw(item.Nombre)</h1>
                    </div>
                </div>
            }
        }

        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
        </button>
    </div>
</div> *@







    <!-- Cierra Slider-->
